kind: pipeline
name: default
steps:
  - name: build
    image: rust:1.61.0
    commands:
      - cargo build --all
  - name: publish
    image: rust:1.61.0
    commands:
      - ./publish.sh
    when:
      event:
        - tag